# ============================================================================
# Secrets pour Environnement PROD (Production)
# ============================================================================
# ⚠️  NE JAMAIS COMMITER CE FICHIER
# ⚠️  À CONFIGURER MANUELLEMENT SUR LE SERVEUR DE PRODUCTION
#
# Ce fichier est un TEMPLATE - créez prod.env avec les vraies valeurs de production

# Database - REMPLACER PAR LES VRAIES VALEURS DE PRODUCTION
# Utiliser des credentials forts et uniques
DATABASE_URL=postgresql://prod_user:STRONG_PASSWORD_HERE@prod-db-host:5432/saas_platform_prod
TENANT_DATABASE_URL_TEMPLATE=postgresql://prod_user:STRONG_PASSWORD_HERE@prod-db-host:5432/{database_name}

# JWT - GÉNÉRER UNE CLÉ FORTE UNIQUE POUR LA PRODUCTION
# ⚠️  CRITIQUE: Cette clé signe tous les tokens JWT
# Générer avec: JWT_SECRET_KEY=$(head -c 32 /dev/urandom | xxd -p -c 64)

# NE JAMAIS réutiliser la clé de dev/docker
JWT_SECRET_KEY=REPLACE_WITH_STRONG_RANDOM_KEY_FROM_OPENSSL_RAND_HEX_32
JWT_ACCESS_TOKEN_EXPIRES=900
JWT_REFRESH_TOKEN_EXPIRES=604800

# S3 - REMPLACER PAR LES VRAIES VALEURS AWS/S3
# Pour AWS S3, l'endpoint est généralement https://s3.amazonaws.com
# ou régional comme https://s3.eu-west-1.amazonaws.com
S3_ENDPOINT_URL=https://s3.amazonaws.com
S3_ACCESS_KEY_ID=REPLACE_WITH_AWS_ACCESS_KEY_ID
S3_SECRET_ACCESS_KEY=REPLACE_WITH_AWS_SECRET_ACCESS_KEY
S3_BUCKET=saas-documents-production
S3_REGION=us-east-1

# ============================================================================
# CHECKLIST DE SÉCURITÉ POUR LA PRODUCTION
# ============================================================================
# ✅ JWT_SECRET_KEY généré avec openssl rand -hex 32
# ✅ Database credentials avec mots de passe forts (20+ caractères)
# ✅ S3 credentials avec permissions minimales (principe du moindre privilège)
# ✅ Ce fichier NE doit JAMAIS être commité dans Git
# ✅ Sauvegarder ce fichier de manière chiffrée (GPG) hors du repository
# ✅ Utiliser des secrets différents entre dev/docker/prod
# ✅ Planifier la rotation régulière des credentials (tous les 90 jours)
